#ifndef TASKS_HPP
#define TASKS_HPP

#include <cstddef>

namespace prisyach
{

int isDuplicateCharacters(const char* str, size_t sizeOfString);

char* mergeStrings(const char* str1, const char* str2, size_t sizeStr1);

}

#endif
